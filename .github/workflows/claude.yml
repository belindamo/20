name: Researcher

on:
  pull_request:
    types: [opened, synchronize]

jobs:
  lit-review:
    if: startsWith(github.head_ref, 'lit-review/')
    runs-on: ubuntu-latest
    permissions:
      contents: read
      pull-requests: read
      id-token: write
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          fetch-depth: 1

      - name: Literature Review
        uses: anthropics/claude-code-action@beta
        with:
          anthropic_api_key: ${{ secrets.ANTHROPIC_API_KEY }}
          timeout_minutes: "60"
          direct_prompt: |
            You are a research assistant following CS197 methodology. 
            Focus on comprehensive paper review using Arxiv.
          mcp_config: |
            {
              "mcpServers": {
                "search": {
                  "command": "npx",
                  "args": ["-y", "open-websearch@latest"]
                }
              }
            }
          allowed_tools: "mcp__search__search"
          # mcp_config: |
          #   {
          #     "mcpServers": {
          #       "sequential-thinking": {
          #         "command": "npx",
          #         "args": [
          #           "-y",
          #           "@modelcontextprotocol/server-sequential-thinking"
          #         ]
          #       }
          #     }
          #   }
          # allowed_tools: "mcp__sequential-thinking__sequentialthinking"             
          # mcp_config: |
          #   {
          #     "mcpServers": {
          #       "exa": {
          #         "command": "npx",
          #         "args": ["-y", "exa-mcp-server"],
          #         "env": {
          #           "EXA_API_KEY": "${{ secrets.EXA_API_KEY }}"
          #         }
          #       }
          #     }
          #   }
          # allowed_tools: "mcp__exa__web_search_exa"          
          # mcp_config: |
          #   {
          #     "mcpServers": {
          #       "semantic-scholar": {
          #         "command": "uvx",
          #         "args": ["semantic-scholar-mcp"]
          #         # "env": {
          #         #   "SEMANTIC_SCHOLAR_API_KEY": "${{ secrets.SEMANTIC_SCHOLAR_API_KEY }}"
          #         # }
          #       }
          #     }
          #   }
          # allowed_tools: "mcp__semantic-scholar__search_papers"
          # allowed_tools: "mcp__semantic-scholar__search_papers,mcp__semantic-scholar__get_paper,mcp__semantic-scholar__get_author,mcp__semantic-scholar__get_paper_citations,mcp__semantic-scholar__get_paper_references,mcp__semantic-scholar__search_authors,mcp__semantic-scholar__get_recommendations"

